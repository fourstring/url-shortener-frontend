// import React from "react";
// import {ICart, ICartItem} from "../types/ICart";
// import {useCheckboxes} from "../hooks/useCheckboxes";

// export function Cart(props: React.PropsWithoutRef<{ cart: ICart }>) {
//   const {selected, checkboxes, select} = useCheckboxes<ICartItem>(props.cart.items);

// }